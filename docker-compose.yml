services:
  web-flask:
    restart: always
    build:
      context: .
      dockerfile: ./docker-emlo/Dockerfile-flask
    environment:
      SOLR_URL: "localhost"
    expose:
      - "5000"
    ports:
      - 5000:5000
    command: python run.py

  solr:
    build: ./docker-emlo/solr-emlo
    #restart: no
    environment:
      SOLR_LOG_LEVEL: INFO
      LD_BIND_NOW: 1
    ports:
      - "8983:8983"
    volumes:
      - ./docker-emlo/solr-conf/solr/home:/opt/solr/server/solr/home
      - ./docker-emlo/solr-data/:/data-solr/
    ulimits:
      nofile:
        soft: "65536"
        hard: "65536"
